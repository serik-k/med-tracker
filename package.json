{
  "name": "med-tracker-workspace",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20",
    "npm": ">=10"
  },
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix client install",
    "ci:install": "npm --prefix server ci && npm --prefix client ci",
    "start:server": "npm --prefix server start",
    "dev:server": "npm --prefix server dev",
    "dev:client": "npm --prefix client dev",
    "check:server": "npm --prefix server run check",
    "test:server": "npm --prefix server test",
    "typecheck:client": "node client/node_modules/vue-tsc/bin/vue-tsc.js --noEmit -p client/tsconfig.json",
    "build:client": "npm --prefix client run build",
    "check": "npm run check:server && npm run test:server && npm run typecheck:client && npm run build:client"
  }
}
